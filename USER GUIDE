This will guide you through the preparaion of the enveronment to run the code

## REQUIREMENTS
Python version 3.0 to above
To check your current version: python --version
To update python: 

## Install Dependencies
pip install biopython

## Prepare the enveronment
1. Create a directory
2. Create a Protein.txt file with amino acid seqience in fasta format (Example file given)
3. Save ProCharg_cmd.py in the directory
or 
Save ProCharg_UI.py in the directory

##Run with command line
1. Open terminal in the directory
2. Run the code: python ProCharg_cmd.py

##Use with GUI
1. Open terminal in the directory
2. Run the code: python ProCharg_UI.py
